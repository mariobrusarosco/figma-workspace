import type { SlotContent } from '../types/slot.types'

export const syncSlotContent = async (contents: Array<SlotContent>) => {
  // TODO: Implement smart sync logic in a later phase.
  return contents
}

