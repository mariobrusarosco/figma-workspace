import type { NavigationCategory } from '../types/navigation.types'

export const scanNavigationStructure = async (): Promise<Array<NavigationCategory>> => {
  // TODO: Implement file scanning logic in a later phase.
  return []
}

